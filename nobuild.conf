[main]
name=Folte Linux
version=1.0
description=Improve, Minimal

[build]
filename=folte-${version}-${arch}.iso

[base]
distro=debian
version=trixie
architecture=amd64

[extra]
mirror=http://httpredir.debian.org/debian/
archive_areas=main contrib non-free non-free-firmware